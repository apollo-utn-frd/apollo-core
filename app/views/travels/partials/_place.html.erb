<div class="border-top border-bottom p-4">
    <h6>
        <div class="row">
            <div class="col-md-2">
                <strong class="marker-name"><%= (index + 97).chr %></strong>
            </div>
            <div class="col-md-10 d-flex">
                <strong><%= place.title %></strong>
            </div>
        </div>
    </h6>
    <p class="m-0"><%= place.description %></p>
</div>